/// <reference types="bun" />
// Pulls in Bun's globals and the "bun:test" module types. Kept as a reference
// file rather than compilerOptions.types so TypeScript still auto-includes the
// other @types packages Next relies on.
