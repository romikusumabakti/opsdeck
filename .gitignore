# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.bun

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# terminal sidecar bundle — written to the repo root by the Dockerfile's
# `bun build lib/terminal/main.ts --outfile terminal-server.js` step (and by
# running that command locally to verify it)
/terminal-server.js

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
# Added by code-review-graph
.code-review-graph/

# Personal Claude Code overrides (per-dev permissions, not team config)
.claude/settings.local.json

# Runtime lock (per-session, not team config)
.claude/scheduled_tasks.lock
